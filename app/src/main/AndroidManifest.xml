<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dss.hrms">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.change_pass.view.ChangePassActivity"></activity>
        <activity android:name=".activity.ac_otp.view.OTPActivity" />
        <activity android:name=".activity.ac_forget_pass.view.ForgetPAssActivity" />
        <activity android:name=".activity.ac_base.view.BaseActivity" />
        <activity android:name=".activity.ac_login_signup.view.LoginSignupActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>